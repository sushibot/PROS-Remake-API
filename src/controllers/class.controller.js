const express = require("express");
const course_service = require("../services/course.service");

const create_class = async (req = express.request, res = express.response) => {
    try {

    } catch (error) {
        return error;
    }
}

module.exports = {
    create_class
}